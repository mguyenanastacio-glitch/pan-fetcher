services:
  pan-fetcher:
    # Use Docker Hub or GHCR image:
    # image: mguyenanastacio/pan-fetcher:latest
    image: ghcr.io/mguyenanastacio-glitch/pan-fetcher:latest
    container_name: pan-fetcher
    restart: unless-stopped
    ports:
      - "8115:8115"
    volumes:
      - ./data:/app/data
      - ./config.toml:/app/config.toml
    environment:
      - TZ=Asia/Shanghai
