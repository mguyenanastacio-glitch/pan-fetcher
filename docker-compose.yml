version: "3.8"

services:
  pan-fetcher:
    image: ghcr.io/mguyenanastacio-glitch/pan-fetcher:latest
    container_name: pan-fetcher
    restart: unless-stopped
    ports:
      - "8115:8115"
    volumes:
      - ./data:/app/data
      - ./config.toml:/app/config.toml:ro
    environment:
      - TZ=Asia/Shanghai
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8115/api/logs"]
      interval: 30s
      timeout: 5s
      retries: 3
